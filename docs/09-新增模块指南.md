# 新增模块指南

本文档是面向开发者和 AI 工具的完整操作手册，提供从零新增一个业务模块的全部步骤。以新增一个「文章管理」模块为例，完整演示。

## 前置约定

- 资源名: `articles`
- 数据库表: `sys_article`
- 权限前缀: `content:article`
- 菜单路径: `/system/article`（或自定义模块路径如 `/content/article`）

## Step 1: 数据库 Schema

### 1.1 创建 Schema 文件

```ts
// src/db/schema/article.ts
import { sql } from "drizzle-orm";
import {
  bigint,
  index,
  mysqlTable,
  text,
  tinyint,
  varchar,
} from "drizzle-orm/mysql-core";
import { localDatetime } from "../custom-types";

export const sysArticle = mysqlTable(
  "sys_article",
  {
    id: bigint("id", { mode: "number", unsigned: true })
      .primaryKey()
      .autoincrement(),
    title: varchar("title", { length: 200 }).notNull(),
    content: text("content"),
    category: varchar("category", { length: 50 }).notNull().default(""),
    status: tinyint("status", { unsigned: true }).notNull().default(1), // 0-草稿 1-发布
    remark: varchar("remark", { length: 500 }),
    createdAt: localDatetime("created_at")
      .notNull()
      .default(sql`CURRENT_TIMESTAMP`),
    updatedAt: localDatetime("updated_at")
      .notNull()
      .default(sql`CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`),
  },
  (table) => ({
    statusIdx: index("idx_status").on(table.status),
    categoryIdx: index("idx_category").on(table.category),
  }),
);

export type SysArticle = typeof sysArticle.$inferSelect;
export type NewSysArticle = typeof sysArticle.$inferInsert;
```

### 1.2 注册 Schema

```ts
// src/db/schema/index.ts — 追加导出
export * from "./article";
```

### 1.3 同步数据库

```bash
pnpm db:push
```

## Step 2: 服务端 — Service 层

### 2.1 创建查询文件

```ts
// src/server/services/system/article/article.query.ts
import { eq, like, sql } from "drizzle-orm";
import { db } from "@/db";
import { sysArticle } from "@/db/schema";
import type { ArticleQuery } from "@/server/routes/articles/dtos";

export async function getArticleList(query: ArticleQuery) {
  const conditions = [];
  if (query.title) conditions.push(like(sysArticle.title, `%${query.title}%`));
  if (query.status !== undefined)
    conditions.push(eq(sysArticle.status, query.status));
  if (query.category) conditions.push(eq(sysArticle.category, query.category));

  const where =
    conditions.length > 0
      ? sql`${sql.join(conditions, sql` AND `)}`
      : undefined;
  const page = query.page || 1;
  const pageSize = query.pageSize || 20;

  const [list, countResult] = await Promise.all([
    db
      .select()
      .from(sysArticle)
      .where(where)
      .limit(pageSize)
      .offset((page - 1) * pageSize),
    db
      .select({ count: sql<number>`count(*)` })
      .from(sysArticle)
      .where(where),
  ]);

  return { list, total: countResult[0].count };
}

export async function findArticleById(id: number) {
  return db.query.sysArticle.findFirst({ where: eq(sysArticle.id, id) });
}
```

### 2.2 创建业务逻辑文件

```ts
// src/server/services/system/article/article.service.ts
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { sysArticle } from "@/db/schema";
import { handleDatabaseError, NotFoundError } from "@/lib/errors";
import type {
  CreateArticleInput,
  UpdateArticleInput,
} from "@/server/routes/articles/dtos";
import { findArticleById } from "./article.query";

export async function getArticleById(id: number) {
  const article = await findArticleById(id);
  if (!article) throw new NotFoundError("文章", id);
  return article;
}

export async function createArticle(input: CreateArticleInput) {
  try {
    const [result] = await db.insert(sysArticle).values(input);
    return getArticleById(Number(result.insertId));
  } catch (err) {
    throw handleDatabaseError(err);
  }
}

export async function updateArticle(id: number, input: UpdateArticleInput) {
  await getArticleById(id); // 确认存在
  try {
    await db.update(sysArticle).set(input).where(eq(sysArticle.id, id));
    return getArticleById(id);
  } catch (err) {
    throw handleDatabaseError(err);
  }
}

export async function deleteArticle(id: number) {
  await getArticleById(id);
  await db.delete(sysArticle).where(eq(sysArticle.id, id));
}
```

### 2.3 创建索引文件

```ts
// src/server/services/system/article/index.ts
export { getArticleList } from "./article.query";
export {
  getArticleById,
  createArticle,
  updateArticle,
  deleteArticle,
} from "./article.service";
```

### 2.4 注册到服务导出

```ts
// src/server/services/index.ts — 追加
export * from "./system/article";
```

## Step 3: 服务端 — 路由层

### 3.1 创建 DTOs

```ts
// src/server/routes/articles/dtos.ts
import { z } from "zod";
import { createPaginatedSchema, PaginationQuerySchema } from "../shared";

export const ArticleSchema = z.object({
  id: z.number(),
  title: z.string(),
  content: z.string().nullable(),
  category: z.string(),
  status: z.number(),
  remark: z.string().nullable(),
  createdAt: z.string(),
  updatedAt: z.string(),
});

export const CreateArticleInputSchema = z.object({
  title: z.string().min(1).max(200),
  content: z.string().optional(),
  category: z.string().max(50).optional(),
  status: z.number().int().min(0).max(1).optional(),
  remark: z.string().max(500).optional(),
});

export const UpdateArticleInputSchema = CreateArticleInputSchema.partial();

export const ArticleQuerySchema = PaginationQuerySchema.extend({
  title: z.string().optional(),
  category: z.string().optional(),
  status: z.coerce.number().int().min(0).max(1).optional(),
});

export const PaginatedArticleSchema = createPaginatedSchema(ArticleSchema);

// 类型导出
export type Article = z.infer<typeof ArticleSchema>;
export type CreateArticleInput = z.infer<typeof CreateArticleInputSchema>;
export type UpdateArticleInput = z.infer<typeof UpdateArticleInputSchema>;
export type ArticleQuery = z.infer<typeof ArticleQuerySchema>;
export type PaginatedArticle = z.infer<typeof PaginatedArticleSchema>;
```

### 3.2 创建路由实现

```ts
// src/server/routes/articles/index.ts
import { Hono } from "hono";
import { zValidator } from "@hono/zod-validator";
import type { Env } from "@/server/context";
import { auditLog, requireAuth, requirePermission } from "@/server/middleware";
import { R } from "@/server/utils/response";
import {
  getArticleList,
  getArticleById,
  createArticle,
  updateArticle,
  deleteArticle,
} from "@/server/services";
import { IdParamSchema } from "../shared";
import {
  ArticleQuerySchema,
  CreateArticleInputSchema,
  UpdateArticleInputSchema,
} from "./dtos";

export const articles = new Hono<Env>()
  .use("/*", requireAuth)
  .get(
    "/",
    requirePermission("content:article:list"),
    zValidator("query", ArticleQuerySchema),
    async (c) => R.ok(await getArticleList(c.req.valid("query"))),
  )
  .get(
    "/:id",
    requirePermission("content:article:query"),
    zValidator("param", IdParamSchema),
    async (c) => R.ok(await getArticleById(c.req.valid("param").id)),
  )
  .post(
    "/",
    requirePermission("content:article:create"),
    auditLog({
      module: "文章管理",
      operation: "创建",
      description: "创建文章",
    }),
    zValidator("json", CreateArticleInputSchema),
    async (c) => R.created(await createArticle(c.req.valid("json"))),
  )
  .put(
    "/:id",
    requirePermission("content:article:update"),
    auditLog({
      module: "文章管理",
      operation: "更新",
      description: "更新文章",
    }),
    zValidator("param", IdParamSchema),
    zValidator("json", UpdateArticleInputSchema),
    async (c) =>
      R.ok(await updateArticle(c.req.valid("param").id, c.req.valid("json"))),
  )
  .delete(
    "/:id",
    requirePermission("content:article:delete"),
    auditLog({
      module: "文章管理",
      operation: "删除",
      description: "删除文章",
    }),
    zValidator("param", IdParamSchema),
    async (c) => {
      await deleteArticle(c.req.valid("param").id);
      return R.success("删除成功");
    },
  );
```

### 3.3 注册路由

```ts
// src/server/route-defs.ts — 追加
import { articles } from "./routes/articles";

export const routes = new Hono<Env>()
  .route("/auth", auth)
  .route("/admins", admins)
  .route("/roles", roles)
  .route("/menus", menus)
  .route("/operation-logs", operationLogs)
  .route("/configs", configs)
  .route("/articles", articles); // ← 新增

export type AppType = typeof routes;
```

**至此后端 API 已完成，可通过 Swagger UI 测试。**

## Step 4: 客户端 — React Query Hooks

### 4.1 创建 Hooks 文件

```ts
// src/hooks/queries/articles.ts
import { getApiClient } from "@/lib/client";
import type {
  Article,
  ArticleQuery,
  CreateArticleInput,
  PaginatedArticle,
  UpdateArticleInput,
} from "@/server/routes/articles/dtos";
import { createResource } from "./core";

const getClient = () => getApiClient().articles;

const articleResource = createResource<
  PaginatedArticle,
  Article,
  CreateArticleInput,
  UpdateArticleInput,
  ArticleQuery
>({
  resourceName: "articles",
  getClient: getClient as any,
  messages: {
    list: "获取文章列表失败",
    detail: "获取文章详情失败",
    create: "创建文章失败",
    update: "更新文章失败",
    delete: "删除文章失败",
  },
});

export const useArticleList = articleResource.useList;
export const useArticleDetail = articleResource.useDetail;
export const useCreateArticle = articleResource.useCreate;
export const useUpdateArticle = articleResource.useUpdate;
export const useDeleteArticle = articleResource.useDelete;
export const articleKeys = articleResource.keys;
```

### 4.2 注册 Query Keys

```ts
// src/hooks/queries/core/query-keys.ts — 追加
export const queryKeys = {
  // ...existing
  articles: createQueryKeys("articles"),
};
```

### 4.3 导出 Hooks

```ts
// src/hooks/queries/index.ts — 追加
export * from "./articles";
```

## Step 5: 客户端 — 页面

### 5.1 创建页面目录

```
src/app/(dashboard)/content/
└── article/
    ├── page.tsx          # 列表页
    ├── components/
    │   ├── article-table.tsx    # 表格组件
    │   └── article-form.tsx     # 创建/编辑表单
    └── ...
```

### 5.2 列表页示例结构

```tsx
// src/app/(dashboard)/content/article/page.tsx
"use client";

import { useState } from "react";
import { useArticleList, useDeleteArticle } from "@/hooks/queries";
import { PermissionGuard } from "@/components/permission-guard";

export default function ArticlePage() {
  const [params, setParams] = useState({ page: 1, pageSize: 20 });
  const { data, isLoading } = useArticleList(params);
  const deleteArticle = useDeleteArticle();

  return (
    <>
      {/* 搜索栏 */}
      {/* 操作按钮 */}
      <PermissionGuard permission="content:article:create">
        <Button>新增文章</Button>
      </PermissionGuard>
      {/* 数据表格 */}
      {/* 分页 */}
    </>
  );
}
```

## Step 6: 配置菜单权限

通过管理后台的「菜单管理」功能添加：

### 6.1 添加目录（如需要）

| 字段     | 值       |
| -------- | -------- |
| 菜单类型 | 目录 (D) |
| 菜单名称 | 内容管理 |
| 图标     | FileText |
| 排序     | 200      |

### 6.2 添加菜单

| 字段     | 值               |
| -------- | ---------------- |
| 父级     | 内容管理         |
| 菜单类型 | 菜单 (M)         |
| 菜单名称 | 文章管理         |
| 路由路径 | /content/article |
| 图标     | BookOpen         |

### 6.3 添加按钮权限

| 菜单名称 | 权限标识                 | 菜单类型 |
| -------- | ------------------------ | -------- |
| 查询列表 | `content:article:list`   | 按钮 (B) |
| 查询详情 | `content:article:query`  | 按钮 (B) |
| 创建文章 | `content:article:create` | 按钮 (B) |
| 编辑文章 | `content:article:update` | 按钮 (B) |
| 删除文章 | `content:article:delete` | 按钮 (B) |

### 6.4 分配权限

在「角色管理」中，为相应角色勾选新添加的菜单和按钮权限。

## 完整检查清单

| #   | 步骤              | 文件                                                    | 状态 |
| --- | ----------------- | ------------------------------------------------------- | ---- |
| 1   | 数据库 Schema     | `src/db/schema/article.ts`                              | □    |
| 2   | Schema 导出       | `src/db/schema/index.ts`                                | □    |
| 3   | 同步数据库        | `pnpm db:push`                                          | □    |
| 4   | Service 查询      | `src/server/services/system/article/article.query.ts`   | □    |
| 5   | Service 业务逻辑  | `src/server/services/system/article/article.service.ts` | □    |
| 6   | Service 导出      | `src/server/services/system/article/index.ts`           | □    |
| 7   | Service 全局导出  | `src/server/services/index.ts`                          | □    |
| 8   | 路由 DTOs         | `src/server/routes/articles/dtos.ts`                    | □    |
| 9   | 路由实现          | `src/server/routes/articles/index.ts`                   | □    |
| 10  | 路由注册          | `src/server/route-defs.ts`                              | □    |
| 11  | React Query Hooks | `src/hooks/queries/articles.ts`                         | □    |
| 12  | Query Keys 注册   | `src/hooks/queries/core/query-keys.ts`                  | □    |
| 13  | Hooks 导出        | `src/hooks/queries/index.ts`                            | □    |
| 14  | 页面组件          | `src/app/(dashboard)/content/article/page.tsx`          | □    |
| 15  | 菜单配置          | 管理后台 → 菜单管理                                     | □    |
| 16  | 权限分配          | 管理后台 → 角色管理                                     | □    |
