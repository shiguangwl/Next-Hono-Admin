# 部署配置模板（复制为 .deploy.env）
# 多实例部署：为每个实例创建 .deploy.<name>.env
#
# [SSH 连接]
SSH_HOST=your_server_ip # 远程服务器地址
SSH_PORT=22 # SSH 端口
SSH_USER=root # SSH 用户名
SSH_KEY_PATH= # SSH 私钥路径（推荐）
SSH_PASSWORD= # SSH 密码（仅无密钥时使用）
#
# [远程目录]
REMOTE_DIR=/opt/app # 远程部署目录
#
# [镜像]
IMAGE_NAME=next_hono_admin_service # 镜像名称
IMAGE_TAG= # 镜像标签（空=自动时间戳）
IMAGE_RETENTION_COUNT=5 # 镜像保留数量
#
# [容器]
CONTAINER_NAME=app # 容器名称
COMPOSE_PROJECT_NAME= # Compose 项目名称（可选）
APP_PORT=3000 # 宿主机端口
#
# [Compose]
COMPOSE_FILE=docker-compose.yml # Compose 文件路径
ENV_FILES=.env.production # 环境变量文件（逗号分隔）
#
# [多实例示例]
# minute: REMOTE_DIR=/opt/app_minute CONTAINER_NAME=app_minute APP_PORT=3000 ENV_FILES=.env.production.minute
# daily:  REMOTE_DIR=/opt/app_daily  CONTAINER_NAME=app_daily  APP_PORT=3001 ENV_FILES=.env.production.daily
# deploy: ./deploy.sh --config .deploy.minute.env  # ./deploy.sh --config .deploy.daily.env
